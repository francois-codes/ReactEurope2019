import React from "react";
import { AppRegistry } from "react-native";

// this file is generated by react-bundle-aggregator

<% reactModules.forEach(function(module) { %>
  import <%= module.moduleName %> from "<%= module.package %>";
<%}); %>

<% reactModules.forEach(function(module) { %>
  AppRegistry.registerComponent("<%= module.moduleName %>",() => <%= module.moduleName %>);
<%}); %>

